This project is used to generate self signed certificate, based on [Soarez's blog](https://gist.github.com/Soarez/9688998).

Run `make init` before `make client service=something`, where the parameter `service` is required to distinguish certificates.
